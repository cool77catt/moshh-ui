// Setup directory/file paths
import RNFS from 'react-native-fs';

// Setup Video Settings
export const VIDEO_DIRECTORY = RNFS.DocumentDirectoryPath + '/videos';
export const VIDEO_EXTENSION = '.mov';
